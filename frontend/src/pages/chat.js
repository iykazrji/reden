import React, { Fragment } from 'react';
import Styled from 'styled-components';
import { observer, inject } from 'mobx-react';

const ChatPage = (props) => {
	return <Fragment>ChatPage</Fragment>;
};

export default ChatPage;
